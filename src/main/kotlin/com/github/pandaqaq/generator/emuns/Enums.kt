package com.github.pandaqaq.generator.emuns

enum class ActivityInitParam {
    FULLSCREEN,
    WITH_STATE,
    BOTH,
    NONE
}

enum class FragmentInitParam {
    FULLSCREEN,
    WITH_STATE,
    BOTH,
    NONE
}

enum class PageType {
    NORMAL,
    REFRESH
}

enum class ItemType {
    SINGLE,
    MULTI,
    NONE
}